{
  "name": "functions",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npx kill-port 9000 -y && npm run build && cd .. && firebase emulators:start --project ssrtest-cebd9",
    "shell": "npm run build && cd .. && firebase functions:shell --project ssrtest-cebd9",
    "start": "npm run shell",
    "deploy": "npm run build && cd .. && firebase deploy --only functions --project ssrtest-cebd9",
    "logs": "cd .. && firebase functions:log --project ssrtest-cebd9"
  },
  "engines": {
    "node": "18"
  },
  "main": "lib/index.js",
  "dependencies": {
    "dotenv": "^17.4.2",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.7.0",
    "sharp": "^0.34.5",
    "uuid": "^14.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "@types/sharp": "^0.31.1",
    "firebase-tools": "^15.18.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.6.0"
  },
  "private": true
}
